{{ if eq .flavor "work" -}}
# packages/Brewfile.work — applied only when flavor == "work".
# Add work-specific tools as you set up the new role. Examples:
#
#   cask "slack"
#   cask "zoom"
#   cask "visual-studio-code"
#   brew "kubectl"
#   brew "terraform"
#   mas "Microsoft Teams", id: 1113153706
#
# Discover IDs for Mac App Store apps via:
#   mas search "<app name>"
{{- end }}
